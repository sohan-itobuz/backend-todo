todo.js =>{
  after timestamp true ->
  toJSON: {
      virtuals: true,
      transform: (doc, ret) => {
        ret.id = ret._id; // Map Mongoose's _id to 'id' 
        delete ret._id;
        delete ret.__v;
      },
    },
    toObject: { virtuals: true },
}

controller.js =>{
  inside update task->
  { $set: req.body },
}